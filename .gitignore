.venv/
__pycache__/
*.py[cod]
data/raw/
# Build output. dist/index.html is the production file served by Pages and
# IS tracked; everything else (artifact.html, the static SVGs) is disposable.
dist/*
!dist/index.html
.env
.DS_Store
